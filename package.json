{
  "name": "jdev2017-a04",
  "scripts": {
    "start": "$(npm bin)/reveal-md -w slides.md",
    "build": "$(npm bin)/reveal-md slides.md -S",
    "build:pdf": "$(npm bin)/reveal-md slides.md --print"
  },
  "dependencies": {
    "reveal-md": "^0.3.3"
  }
}
